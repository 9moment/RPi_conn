import urllib2
import random
import time
if __name__ =='__main__':
	data = [random.random() for i in range(10)]
	for d in data:
		url = "http://dweet.io/dweet/for/mantour?value=%f'
		resp = urllib2.urlopen(url%d)
		print(resp.read())
		time.sleep(10)
